#!/bin/sh
set -e

# Update XFCE desktop & mime targets
if [ -x /usr/bin/update-desktop-database ]; then
    update-desktop-database /usr/share/applications/
fi

if [ -x /usr/bin/update-mime-database ]; then
    update-mime-database /usr/share/mime/
fi

